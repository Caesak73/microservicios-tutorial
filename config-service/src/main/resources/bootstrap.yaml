server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git: 
          default-label: zipkin-sleuth,circuit-breaker,multiple-instances,config-server,gateway
          uri: https://github.com/Caesak73/microservicios-tutorial.git
          search-paths: config-data
  application:
      name: config-service
          
    
